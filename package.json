{
    "name": "dynamonster",
    "module": "lib/index.ts",
    "type": "module",
    "devDependencies": {
        "@types/bun": "latest",
        "prettier": "^3.8.3"
    },
    "scripts": {
        "format": "prettier --write ."
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.1026.0",
        "@aws-sdk/util-dynamodb": "^3.996.2"
    }
}
